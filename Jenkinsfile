flow {
  BOOLKEY = false

  PR = { conf ->
    println "pr started"
    println "${conf.inspect()}"
    action(conf)
    println "pr done"
  }

  RELEASE = {
    println "release started"
    println "release done"
  }
}